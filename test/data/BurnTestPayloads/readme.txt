This folder contains items that are used in Burn feature tests.
This includes sample products (MSIs & EXEs), patches (MSPs).

Both the items and their corresponding source code are included.

The items are pre-built and checked in to allow easy configuration of a web server to test downloading of items too.
