namespace Wix.Samples
{
    /// <summary>
    /// Result codes.
    /// </summary>
    public enum BundleResult
    {
        Error = -1,
        None,
        Ok,
        Cancel,
        Abort,
        Retry,
        Ignore,
        Yes,
        No,
        Close,
        Help,
        TryAgain,
        Continue,
    }
}
